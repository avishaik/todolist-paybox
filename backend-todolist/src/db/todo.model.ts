export const c = 7;
/*

import { Model, model } from "mongoose";
import { ITodo } from "../../../common/todo.model";
import { todoSchema } from "./todoSchema";


export interface TodoInterface extends Document {

}

export interface TodoModelInterface extends Model<ITodo> {
    retrieveTodoList(): ITodo[];
    getTodo(id: string): ITodo;
    saveTodo(todo: ITodo): void;
}

todoSchema.statics.retrieveTodoList = async function() {
    try {
        const todoList: ITodo[] = await todoModel
    } catch(e) {

    }
};

export const TodoModel = model<ITodo, TodoInterface>('Todo', todoSchema);

*/