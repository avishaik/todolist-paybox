export const b = 6;
/*

import { Schema } from "mongoose";
import { ITodo } from "../../../common/todo.model";

const TodoSchemaFields = new Schema<ITodo>({
    _id: { type: Schema.Types.ObjectId, required: true},
    title:  { type: String} ,
    description: { type: String },
    Due: { type: Date }
});

export const todoSchema = new Schema(TodoSchemaFields);

*/